export type Breadcrumb = {
  text: string;
  uri?: string;
};

export type MasonryItem = {
  date?: string;
  image: string;
  price?: string;
  snippet?: string;
  tags?: string[];
  title: string;
  uri: string;
};
