import tw from '@/styles';

const styles = tw({
  figure: `
    group/figure
    relative
    select-none
    aspect-square

    base:aspect-auto
    base:select-auto
  `,
});

export default styles;
