import tw from '@/styles';

const styles = {
  button: tw(`
    bg-black/50
    backdrop-blur-sm
    rounded-lg
    cursor-pointer
    duration-500
    text-xs uppercase

    hover:bg-black/10
  `),
};

export default styles;
