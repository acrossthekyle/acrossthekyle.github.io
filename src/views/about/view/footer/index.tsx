import { BackToTop } from '@/ui/footer';

import styles from './stylesheet';

export default function Footer() {
  return (
    <footer className={styles.footer}>
      <BackToTop />
    </footer>
  );
}
