import tw from '@/styles';

const styles = tw({
  container: `
    grid grid-cols-1
    h-auto w-full

    sm:grid-cols-2
    lg:grid-cols-3
    lg:h-full
    lg:flex
  `,
});

export default styles;
