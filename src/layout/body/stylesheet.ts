import tw from '@/styles';

const styles = tw({
  body: `
    antialiased
    flex flex-col
    h-full
    bg-white dark:bg-black
    text-black/90 dark:text-white/90

    base:flex-row
  `,
});

export default styles;
