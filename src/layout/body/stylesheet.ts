import tw from '@/styles';

const styles = tw({
  container: `
    antialiased
    flex flex-col
    h-auto min-h-dvh
    text-(--background)
    bg-(--foreground)
    p-1
    overflow-x-hidden
  `,
});

export default styles;
