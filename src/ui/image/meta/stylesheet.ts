import tw from '@/styles';

const styles = tw({
  container: `
    absolute top-4 right-4
    block
    uppercase
    text-xs
    font-semibold font-mono font-stretch-semi-condensed
    tracking-wide

    base:font-bold
  `,
});

export default styles;
