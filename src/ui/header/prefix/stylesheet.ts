import tw from '@/styles';

const styles = tw({
  container: `
    block
    mt-2 mb-2
    leading-[0.775]
    font-mono
    text-base
    uppercase

    lg:text-xl
    lg:inline-block
    lg:mt-0
    lg:mb-0
  `,
});

export default styles;
