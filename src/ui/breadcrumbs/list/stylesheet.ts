import tw from '@/styles';

const styles = tw({
  container: `
    flex flex-col flex-nowrap gap-2
    overflow-hidden
    mb-16 !pb-0

    xs:gap-0
    xs:flex-row
    xs:items-center
  `,
});

export default styles;
