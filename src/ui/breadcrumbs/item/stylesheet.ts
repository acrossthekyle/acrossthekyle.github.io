import tw from '@/styles';

const styles = tw({
  container: `
    inline-block
    text-xs text-current/75
    uppercase truncate
    duration-300

    hover:text-current/100
  `,
});

export default styles;
