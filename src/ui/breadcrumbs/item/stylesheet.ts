import tw from '@/styles';

const styles = tw({
  container: `
    inline-block
    text-xs text-current/50
    uppercase
    duration-300
    truncate

    hover:text-current/100
  `,
});

export default styles;
