import tw from '@/styles';

const styles = tw({
  container: `
    inline-block
    text-sm text-current/50
    uppercase truncate
    duration-300

    hover:text-current/100

    md:text-xs
  `,
});

export default styles;
